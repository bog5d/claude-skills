# Server 47.85.62.133 — Alibaba Cloud Reference

## Quick Facts

| Property | Value |
|----------|-------|
| IP | 47.85.62.133 |
| Provider | Alibaba Cloud (阿里云) |
| Type | 轻量应用服务器 (SWAS) |
| OS | Alibaba Cloud Linux 3 |
| Specs | 2 vCPU, 1.8 GB RAM, 40 GB disk |
| Disk usage | 26 GB / 40 GB (68%) |
| SSH user | root |
| SSH auth | **publickey only** (PasswordAuthentication no) |
| Console | `swas.console.aliyun.com` |

## Running Services

- **PM2: wx-publisher** — Node.js 中继服务器 (port 8787, `/publish` → DeepSeek + 公众号草稿, `/push_telegram` → @AgentToWest)
- **Docker: rustdesk-hbbs/hbbr** — RustDesk 中继服务
- **仓颉 FOS** — `/root/cangjie-fos` (commit ea0b539, backend ports exposed)

## SSH Key Reference

The server only accepts publickey authentication. Current known key pairs:

| Key File | Fingerprint | Status |
|----------|-------------|--------|
| `~/.ssh/hermes_tailscale_47` | `SHA256:gN2ODbIiJG2yRbjBrce0oFMe00wY/mMfd54GD2OOizo` | Generated 2026-06-12, **not yet injected** |

To inject this key: log into Alibaba Cloud SWAS console → the instance → Key Management → Import the public key.

## Console Access

- **Main account login**: `account.aliyun.com` — has slider CAPTCHA, very hard to automate
- **RAM login**: `signin.aliyun.com/root.onaliyun.com/login.htm` — NO CAPTCHA
- **Console URL**: `swas.console.aliyun.com` (轻量服务器专用)
- **Credentials**: The server root password (`Wb88517aliyundl-`) is NOT the same as the Alibaba Cloud console password. Console login requires the Alibaba Cloud main account (email/phone) or RAM user credentials.

## FOS Deployment Notes

- Backend `.env` template: `.env.example` (NOT `.env.production.example`)
- Generated accounts stored in `/root/fos-credentials.txt`
- Docker build OOM risk (1.8 GB RAM too tight for parallel `npm ci` + `apt-get`)
- Mitigation: limit parallel builds or build in stages

## Tailscale Status

- **Not installed** (as of 2026-06-12)
- Auth key provided by user: `tskey-auth-ka68yRU2bv11CNTRL-...` (truncated)
- Blocked by SSH key access — needs key injection first
- Command sequence ready: install Tailscale → join tailnet → configure firewall → verify services
